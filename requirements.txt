# TalkSmith Dependencies
# Core requirements for transcription and diarization

# Core dependencies
numpy>=1.24.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Configuration (built-in to Python, but listed for clarity)
# configparser - included in Python standard library

# Alternative diarization (token-free)
resemblyzer>=0.1.1
scikit-learn>=1.3.0
librosa>=0.10.0

# Audio preprocessing
soundfile>=0.12.1       # Audio I/O
noisereduce>=3.0.0      # Noise reduction
scipy>=1.11.0           # Signal processing (high-pass filter)

# LLM providers for plan generation (optional - install at least one)
anthropic>=0.40.0  # For Claude models
openai>=1.50.0     # For GPT models

# Google Docs integration (optional)
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
google-api-python-client>=2.100.0
